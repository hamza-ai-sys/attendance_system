# Workforce Platform Project Governance

Before planning or editing, read and follow the root `AGENTS.md` and
`docs/development/ai-assisted-development.md` completely. Then read the nearest component
`AGENTS.md` and all task-specific documentation it identifies.

The files under `docs/` are canonical. Adapter and rule files only ensure that the appropriate
documentation is loaded; they do not replace or override it.
