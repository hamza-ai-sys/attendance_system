# Production template. Copy this file to .env.prod at the repository root.
NODE_ENV=production

# PostgreSQL container configuration
POSTGRES_USER=attendance
POSTGRES_PASSWORD=replace-with-a-long-random-database-password
POSTGRES_DB=attendance
POSTGRES_VOLUME_NAME=attendance-postgres-prod

# Internal container connection URL. Keep its credentials synchronized with the values above
# and percent-encode any URI-special characters in the password.
DATABASE_URL=postgresql://attendance:replace-with-a-long-random-database-password@postgres:5432/attendance?schema=public

# Host port bindings used by Docker Compose
DASHBOARD_BIND=127.0.0.1:3000
DEVICE_GATEWAY_BIND=127.0.0.1:4001
POSTGRES_BIND=127.0.0.1:5432

# Dashboard
SESSION_SECRET=replace-with-at-least-32-random-bytes

# Device gateway
DEVICE_SIGNATURE_MAX_AGE_SECONDS=300
