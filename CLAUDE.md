@AGENTS.md

# Claude Code adapter

The imported `AGENTS.md` is the canonical repository instruction source. Before changing files in a
subdirectory, also load and follow that directory's `CLAUDE.md`, which imports its local
`AGENTS.md`.
