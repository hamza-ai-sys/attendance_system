@AGENTS.md

# Claude Code adapter

The imported `AGENTS.md` loads the canonical instructions from
`docs/development/ai-assisted-development.md`. Before changing files in a subdirectory, also load
and follow that directory's `CLAUDE.md`, which imports its local `AGENTS.md` entry point.
